const dimensoes = {
    afastamento: {
        x: 10,
        y: 30,
    },
    altura: 450,
    comprimento: 780,
}

describe("Testando a criação do campo", () => {
    test("Testando a área do campo", () => {
        const Campo = new Campo(dimensoes);

        
    })
})